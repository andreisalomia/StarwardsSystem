services:
  db:
    image: postgres:14.22
    container_name: starwards_db
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${PORT_DB}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${POSTGRES_USER}", "-d", "${POSTGRES_DB}"]
      interval: 15s
      timeout: 15s
      retries: 3
      start_period: 60s
    networks:
      - starwards-network

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: starwards_server
    env_file:
      - .env
    ports:
      - "${PORT_SERVER}:3000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - starwards-network

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: starwards_client
    ports:
      - "${PORT_CLIENT}:5173"
    depends_on:
      - server
    networks:
      - starwards-network

volumes:
  db_data:

networks:
  starwards-network: